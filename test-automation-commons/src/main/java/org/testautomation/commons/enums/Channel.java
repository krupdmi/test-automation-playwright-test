package org.testautomation.commons.enums;

/**
 * Channel through which the test is exercising the system.
 * Extend with project-specific values as needed.
 */
public enum Channel {
    WEB,
    MOBILE,
    API,
    DESKTOP
}
