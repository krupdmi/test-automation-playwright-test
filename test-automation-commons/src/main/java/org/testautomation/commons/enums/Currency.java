package org.testautomation.commons.enums;

/**
 * ISO 4217 currency codes used in test data.
 * Add project-specific currencies as needed.
 */
public enum Currency {
    EUR, USD, GBP, BGN, RON, SEK, NOK, DKK, CHF, PLN
}
