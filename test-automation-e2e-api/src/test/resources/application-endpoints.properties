# ─────────────────────────────────────────────────────────────────────────────
# Endpoint configuration — loaded in all environments via spring.profiles.active
# These values should NOT contain credentials or real environment URLs.
# Environment-specific overrides live in application-{env}.properties
# ─────────────────────────────────────────────────────────────────────────────

# api.base-url.user-service=https://api.example.com/v1/users
# api.base-url.order-service=https://api.example.com/v1/orders
