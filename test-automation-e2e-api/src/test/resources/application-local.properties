# ─────────────────────────────────────────────────────────────────────────────
# Local environment overrides
# Copy this file and fill in your local values — do NOT commit real credentials.
# ─────────────────────────────────────────────────────────────────────────────

# Service URLs (override the commented-out values from application-endpoints.properties)
# api.base-url.user-service=http://localhost:8080/v1/users
# api.base-url.order-service=http://localhost:8080/v1/orders

# Database
# spring.datasource.url=jdbc:postgresql://localhost:5432/testdb
# spring.datasource.username=<your_db_user>
# spring.datasource.password=<your_db_password>
# spring.datasource.driver-class-name=org.postgresql.Driver

# Redis
# spring.data.redis.host=localhost
# spring.data.redis.port=6379
# spring.data.redis.password=
# spring.data.redis.database=0

# Test users
# test.users=user1,user2
# test.users.password=<your_test_password>

# Parallelism
# parallel.threads=2
